## Add a comment indicating the reason for each rejected dependency upgrade added to this list, and what should be done to resolve it (i.e. handle it through a story, etc).
reject: [
  "ioredis", # version 5.7.0 causes "None of startup nodes is available" error in ALS unit tests
  "eslint" # eslint 10.x is not yet supported by @typescript-eslint 8.x (requires ^8.57.0 || ^9.0.0)
]
